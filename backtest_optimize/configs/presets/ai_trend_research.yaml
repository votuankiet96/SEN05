# Placeholder preset for AI Trend research. Override entry/SL/TP in grids.

engine: single
entry:
  model: next_open
  params: {}
stoploss:
  model: atr_multiple
  params:
    atr_mult: 1.5
takeprofit:
  model: risk_multiple
  params:
    r_multiples: [1.0, 2.0, 3.0]
orders:
  model: market
  params: {}
sizing:
  model: fixed_risk_percent
  params:
    account_size: 10000
    risk_per_cluster: 0.01
exits:
  model: none
  params: {}
run:
  ambiguity_policy: conservative
