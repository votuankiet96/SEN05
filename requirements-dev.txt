# Dependencies confirmed actually used by this codebase (see grep of all
# imports across the tree). Superseded by pyproject.toml once the packaging
# stage of the refactor lands; kept here for now so `pytest` is runnable.
#
# Runtime:
pandas==3.0.3
numpy==2.5.1
Flask==3.1.3
pyodbc==5.3.0
python-dotenv==1.2.2

# Dev/test:
pytest==9.1.1

# Note: pyodbc also needs a system ODBC driver manager (unixODBC on
# Linux) plus the actual SQL Server driver (e.g. "ODBC Driver 18 for
# SQL Server") installed at the OS level — pip alone does not provide it.
# This dev VM has neither, so /api/scan fails with a driver-not-found
# error here; that is an environment gap, not a code defect.
